// Cert Tracker — universal dual-pillar presentation layer.
(function initDualPillarUI(global){
  'use strict';
  const CT=global.CertTrackerV3;if(!CT?.dualPillarDepth)return;const esc=CT.util.escapeHtml;

  function style(){if(document.getElementById('ct-dual-pillar-style'))return;const s=document.createElement('style');s.id='ct-dual-pillar-style';s.textContent=`
    .ct-dual-brief{margin:0 0 14px;border:1px solid color-mix(in srgb,var(--accent,#47e8d0) 30%,var(--border,#294252));background:linear-gradient(145deg,color-mix(in srgb,var(--surface,#0b1822) 94%,var(--accent,#47e8d0) 6%),color-mix(in srgb,var(--surface-2,#0d202b) 96%,#000 4%));box-shadow:0 14px 34px rgba(0,0,0,.24),inset 0 1px 0 rgba(255,255,255,.035);border-radius:14px;padding:14px 16px;position:relative;overflow:hidden}
    .ct-dual-brief:before{content:"";position:absolute;inset:0 auto 0 0;width:3px;background:linear-gradient(var(--accent,#47e8d0),transparent)}
    .ct-dual-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.ct-dual-head h3{margin:2px 0 5px;font-size:16px}.ct-dual-kicker{font:700 10px/1.2 ui-monospace,monospace;letter-spacing:.14em;color:var(--accent,#47e8d0)}
    .ct-dual-metrics{display:flex;flex-wrap:wrap;gap:7px;justify-content:flex-end}.ct-dual-metric{border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:6px 8px;background:rgba(0,0,0,.18);font:700 10px/1.2 ui-monospace,monospace;white-space:nowrap}.ct-dual-metric.ct-rank-metric{border-color:color-mix(in srgb,var(--accent,#47e8d0) 42%,rgba(255,255,255,.1));box-shadow:inset 0 0 18px color-mix(in srgb,var(--accent,#47e8d0) 8%,transparent)}.ct-dual-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:10px}.ct-dual-pane{border-top:1px solid rgba(255,255,255,.07);padding-top:9px}.ct-dual-pane b{display:block;margin-bottom:4px;font-size:11px;letter-spacing:.04em}.ct-dual-pane p{margin:0;color:var(--muted,#9eb1bf);font-size:12px;line-height:1.5}.ct-dual-list{margin:6px 0 0;padding-left:18px;color:var(--muted,#9eb1bf);font-size:12px;line-height:1.45}.ct-dual-seq{display:flex;flex-wrap:wrap;gap:6px;margin-top:7px}.ct-dual-seq span{border:1px solid rgba(255,255,255,.08);border-radius:7px;padding:5px 7px;font-size:10px;background:rgba(0,0,0,.15)}
    .ct-role-rank-panel{margin-top:11px;border:1px solid rgba(71,232,208,.14);border-radius:10px;background:rgba(0,0,0,.16);padding:10px}.ct-role-rank-head{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:start}.ct-role-rank-head strong{display:block;font-size:12px}.ct-role-rank-head small{display:block;margin-top:3px;color:var(--muted,#9eb1bf);font-size:10px;line-height:1.4}.ct-role-rank-score{font:800 11px/1 ui-monospace,monospace;border:1px solid rgba(71,232,208,.25);border-radius:999px;padding:6px 8px;white-space:nowrap}.ct-role-ladder{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:5px;margin-top:9px}.ct-role-step{min-width:0;border:1px solid rgba(255,255,255,.08);border-radius:7px;padding:8px 5px;background:rgba(0,0,0,.12);font:800 9px/1 ui-monospace,monospace;text-align:center;color:var(--muted,#9eb1bf)}.ct-role-step.achieved{border-color:rgba(71,232,208,.23);color:var(--text,#e8f2f5)}.ct-role-step.current{border-color:var(--accent,#47e8d0);box-shadow:0 0 16px rgba(71,232,208,.11),inset 0 0 12px rgba(71,232,208,.06);color:var(--accent,#47e8d0)}.ct-role-next{margin-top:8px;color:var(--muted,#9eb1bf);font-size:10px;line-height:1.45}.ct-role-next b{color:var(--text,#e8f2f5)}
    .ct-dual-cert{margin:10px 0;border:1px solid rgba(71,232,208,.18);border-radius:11px;padding:11px;background:rgba(5,18,25,.32)}.ct-dual-cert-title{display:flex;justify-content:space-between;gap:8px;align-items:center;margin-bottom:8px}.ct-dual-cert-title strong{font-size:12px}.ct-dual-pill{border:1px solid rgba(255,255,255,.11);border-radius:999px;padding:4px 7px;font:700 9px/1 ui-monospace,monospace}.ct-dual-cert .ct-dual-grid{margin-top:7px}.ct-dual-standard{margin-top:8px;padding:8px;border-left:2px solid var(--accent,#47e8d0);background:rgba(0,0,0,.15);font-size:11px;line-height:1.45;color:var(--muted,#9eb1bf)}
    @media(max-width:700px){.ct-dual-head{display:block}.ct-dual-metrics{justify-content:flex-start;margin-top:9px}.ct-dual-grid{grid-template-columns:1fr}.ct-dual-brief{padding:12px}.ct-role-rank-head{grid-template-columns:1fr}.ct-role-rank-score{width:max-content}.ct-role-ladder{display:flex;overflow-x:auto;scrollbar-width:none;padding-bottom:2px}.ct-role-ladder::-webkit-scrollbar{display:none}.ct-role-step{flex:0 0 58px}}
  `;document.head.appendChild(s);}

  function currentFilterItem(){return CT.dualPillarDepth.filterItems().find(x=>x.id===(state.filter||'my-path'))||CT.dualPillarDepth.filterItems().find(x=>x.id==='my-path');}
  function rankHtml(rank){if(!rank)return'';const next=rank.next;return `<section class="ct-role-rank-panel" data-role-rank="${esc(rank.key)}"><div class="ct-role-rank-head"><div><div class="ct-dual-kicker">ROLE READINESS</div><strong>${esc(rank.key)} — ${esc(rank.title)}</strong><small>${esc(rank.description)} Readiness is evidence-gated: certifications can improve market access and knowledge, while higher levels increasingly require USED, DESIGNED and OWNED evidence appropriate to the role.</small></div><span class="ct-role-rank-score">${rank.score}% · FLOOR ${rank.floor}%</span></div><div class="ct-role-ladder">${rank.ladder.map(step=>`<span class="ct-role-step${step.achieved?' achieved':''}${step.current?' current':''}" title="${esc(step.title)}">${esc(step.key)}</span>`).join('')}</div>${next?`<div class="ct-role-next"><b>Next: ${esc(next.key)} — ${esc(next.title)}</b><br>${esc(next.gaps.slice(0,4).join(' · ')||'All hard gates met; continue building durable evidence and role scope.')}</div>`:`<div class="ct-role-next"><b>Highest readiness gate reached.</b> Maintain OWNED evidence, strategic scope, commercial judgement and organisational outcomes; the level should fall if the underlying evidence/state no longer supports it.</div>`}</section>`;}
  function pathwayHtml(profile){if(!profile)return'';const top=profile.topCerts.slice(0,5),rank=CT.roleReadiness?.forPathway?.(profile)||null;return `<section class="ct-dual-brief" data-dual-pathway="${esc(profile.id)}"><div class="ct-dual-head"><div><div class="ct-dual-kicker">DUAL-PILLAR PATHWAY</div><h3>${esc(profile.label)}</h3><div class="ct3-muted">${esc(profile.spec.mission)}</div></div><div class="ct-dual-metrics">${rank?`<span class="ct-dual-metric ct-rank-metric">${esc(rank.key)}</span>`:''}<span class="ct-dual-metric">M ${profile.metrics.market}/10</span><span class="ct-dual-metric">K ${profile.metrics.knowledge}/10</span><span class="ct-dual-metric">FLOOR ${profile.metrics.weakerPillar}/10</span><span class="ct-dual-metric">${profile.metrics.deepSubjects} D4/D5 AREAS</span></div></div>${rankHtml(rank)}<div class="ct-dual-grid"><div class="ct-dual-pane"><b>Market-access outcome</b><p>${esc(profile.spec.marketOutcome)}</p></div><div class="ct-dual-pane"><b>Job-performance outcome</b><p>${esc(profile.spec.capabilityOutcome)}</p></div><div class="ct-dual-pane"><b>What the role actually requires</b><ul class="ct-dual-list">${profile.responsibilities.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="ct-dual-pane"><b>Evidence before calling the pathway role-ready</b><ul class="ct-dual-list">${profile.evidence.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div></div>${top.length?`<div class="ct-dual-pane"><b>Current dual-pillar sequence</b><div class="ct-dual-seq">${top.map((x,i)=>`<span>${i+1}. ${esc(x.name)} · M${x.M}/K${x.K}</span>`).join('')}</div></div>`:''}<div class="ct-dual-standard">${esc(profile.roleReadinessRule)} ${esc(profile.sequenceRule)}</div></section>`;}

  function certHtml(cert){const p=CT.dualPillarDepth.certProfile(cert);return `<section class="ct-dual-cert" data-dual-cert="${esc(cert.id)}"><div class="ct-dual-cert-title"><strong>CAREER + PERFORMANCE INTELLIGENCE</strong><span class="ct-dual-pill">M${p.card.M} / K${p.card.K} · BAL ${p.tandem.balance}/10</span></div><div class="ct-dual-metrics" style="justify-content:flex-start"><span class="ct-dual-metric">M${p.card.M} MARKET</span><span class="ct-dual-metric">K${p.card.K} CAPABILITY</span><span class="ct-dual-metric">${p.depth.subjects} SUBJECTS</span><span class="ct-dual-metric">${p.depth.deepCount} D4/D5</span><span class="ct-dual-metric">TARGET ${esc(p.evidenceLevel)}</span></div><div class="ct-dual-grid"><div class="ct-dual-pane"><b>Why it helps you get the role</b><p>${esc(p.marketAccess)}</p></div><div class="ct-dual-pane"><b>Why it helps you perform the role</b><p>${esc(p.capability)}</p></div><div class="ct-dual-pane"><b>Best-fit role contexts</b><p>${esc(p.roleFits.map(x=>`${x.label} ${x.fit}%`).join(' · '))}</p></div><div class="ct-dual-pane"><b>Deepest syllabus areas</b><p>${esc(p.depth.deepSubjects.join(' · ')||'No D4/D5 topic is currently recorded; use the practical conversion tasks to prevent shallow completion.')}</p></div></div><div class="ct-dual-pane"><b>Convert the badge into job performance</b><ul class="ct-dual-list">${p.performanceTasks.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div><div class="ct-dual-standard"><strong>Completion standard:</strong> ${esc(p.completionStandard)}<br><strong>Risk check:</strong> ${esc(p.imbalance)}</div></section>`;}

  const pathwayMarkup=new WeakMap();
  function mountPathway(){
    const content=document.getElementById('tab-content');if(!content)return;
    const existing=content.querySelector(':scope > .ct-dual-brief');
    const tabs=new Set(['dashboard','certifications','roadmap','learning','strategy']);if(!tabs.has(state.currentTab)){existing?.remove();return;}
    const item=currentFilterItem(),profile=item?CT.dualPillarDepth.pathwayProfile(item):null;if(!profile){existing?.remove();return;}
    const html=pathwayHtml(profile);
    // Compare our source markup, not decorated DOM: observers must settle after their own writes.
    if(existing&&pathwayMarkup.get(existing)===html)return;
    const host=document.createElement('div');host.innerHTML=html;const next=host.firstElementChild;
    pathwayMarkup.set(next,html);
    if(existing)existing.replaceWith(next);else content.prepend(next);
  }

  function mountCerts(root=document){
    root.querySelectorAll?.('.cert-row[data-cid]').forEach(row=>{const details=row.querySelector('.cert-details');if(!details||details.querySelector('.ct-dual-cert'))return;const cert=CERTS.find(c=>c.id===row.dataset.cid);if(!cert)return;const host=document.createElement('div');host.innerHTML=certHtml(cert);const resources=details.querySelector('.ct-learning-resources-host');if(resources)details.insertBefore(host.firstElementChild,resources);else details.prepend(host.firstElementChild);});
    root.querySelectorAll?.('.ct-map-cert[data-map-cert]').forEach(node=>{const body=node.querySelector('.ct-map-cert-body');if(!body||body.querySelector('.ct-dual-cert'))return;const cert=CERTS.find(c=>c.id===node.dataset.mapCert);if(cert)body.insertAdjacentHTML('afterbegin',certHtml(cert));});
  }

  function tuneExistingModals(root=document){
    const intel=root.querySelector?.('#ct-intel-modal');if(intel&&!intel.dataset.dualTuned){intel.dataset.dualTuned='1';const sub=intel.querySelector('.ct3-sub');if(sub)sub.textContent='Market access + job-performance capability, practical evidence, role gates and constraints';[...intel.querySelectorAll('h3')].forEach(h=>{if(h.textContent.trim()==='Learning-first sequence')h.textContent='Dual-pillar sequence';});[...intel.querySelectorAll('.ct3-muted')].forEach(n=>{if(/Knowledge and curriculum value lead/i.test(n.textContent))n.textContent='Market access and job-performance capability are co-equal. Timing, readiness, dependencies and practical evidence decide sequencing among strong options.';if(/Curriculum depth and ladder continuity outrank market signal/i.test(n.textContent))n.textContent=n.textContent.replace(/Curriculum depth and ladder continuity outrank market signal;?/i,'Balanced M/K value, dependencies and practical evidence drive the sequence;');});}
    const market=root.querySelector?.('#ct31-market-modal');if(market&&!market.dataset.dualTuned){market.dataset.dualTuned='1';const title=market.querySelector('.ct3-title');if(title)title.textContent='£ Career value & market access';const body=market.querySelector('.ct3-body');if(body)body.insertAdjacentHTML('afterbegin','<div class="ct3-notice"><strong>One pillar only:</strong> this view measures market signalling and role-access economics. Final recommendations also require job-performance capability (K) and practical evidence; a high-value badge is not treated as job readiness.</div>');}
    const career=root.querySelector?.('#ct-career-modal');if(career&&!career.dataset.dualTuned){career.dataset.dualTuned='1';const sub=career.querySelector('.ct3-sub');if(sub)sub.textContent='Market access + capability + evidence + role-transition readiness';}
  }

  const originalWeekly=typeof global.weeklyActions==='function'?global.weeklyActions:null;
  if(originalWeekly&&!originalWeekly.__dualPillarWrapped){const wrapped=function(cert){const original=originalWeekly(cert)||[],p=cert?CT.dualPillarDepth.certProfile(cert):null;if(!p)return original;const urgent=original.find(x=>/exam in|book the exam|dependency/i.test(x));const study=original.find(x=>!/exam in|book the exam|dependency/i.test(x));const practical=p.performanceTasks[0];return [urgent,study,practical?`Performance conversion: ${practical}`:null].filter(Boolean).slice(0,3);};wrapped.__dualPillarWrapped=true;global.weeklyActions=wrapped;}

  let queued=false;function mount(){if(queued)return;queued=true;queueMicrotask(()=>{queued=false;style();mountPathway();mountCerts(document);tuneExistingModals(document);});}
  const app=document.getElementById('app');if(app)new MutationObserver(mount).observe(app,{childList:true,subtree:true});new MutationObserver(mount).observe(document.body,{childList:true,subtree:false});
  global.addEventListener('certtracker:goal-changed',mount);global.addEventListener('certtracker:career-context-changed',mount);global.addEventListener('certtracker:capability-evidence-changed',mount);
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();
  CT.dualPillarUI=Object.freeze({pathwayHtml,certHtml,rankHtml,mount});
})(window);
