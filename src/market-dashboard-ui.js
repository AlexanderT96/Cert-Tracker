// Cert Tracker — primary-dashboard current market value, active-role readiness and live job matches.
(function initMarketDashboardUI(global){
  'use strict';
  const CT=global.CertTrackerV3;if(!CT?.marketReadiness||!CT?.jobMarket)return;const esc=CT.util.escapeHtml;
  let lastFeed=null,feedVersion=0;
  const dashboardMarkup=new WeakMap();
  const dashboardInputs=new WeakMap();let revision=0;
  for(const name of ['certtracker:career-context-changed','certtracker:capability-evidence-changed','certtracker:goal-changed'])global.addEventListener(name,()=>{revision++;});
  function money(v){return CT.marketReadiness.money(v);}
  function style(){if(document.getElementById('ct-market-dashboard-style'))return;const s=document.createElement('style');s.id='ct-market-dashboard-style';s.textContent=`
    .ct-market-now{margin:0 0 14px;border:1px solid rgba(71,232,208,.24);border-radius:14px;background:linear-gradient(150deg,rgba(7,23,31,.96),rgba(8,32,39,.88));box-shadow:0 16px 38px rgba(0,0,0,.25),inset 0 1px 0 rgba(255,255,255,.035);padding:15px;position:relative;overflow:hidden}.ct-market-now:after{content:"";position:absolute;inset:auto -12% -70% 32%;height:150%;background:radial-gradient(circle,rgba(71,232,208,.08),transparent 62%);pointer-events:none}.ct-market-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start;position:relative;z-index:1}.ct-market-kicker{font:700 10px/1.2 ui-monospace,monospace;letter-spacing:.15em;color:var(--accent,#47e8d0)}.ct-market-value{font:800 clamp(24px,4vw,38px)/1.05 ui-monospace,monospace;margin:5px 0}.ct-market-sub{color:var(--muted,#9eb1bf);font-size:12px;line-height:1.45}.ct-market-meta{display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end}.ct-market-chip{border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:6px 8px;background:rgba(0,0,0,.18);font:700 10px/1.2 ui-monospace,monospace}.ct-market-grid{display:grid;grid-template-columns:minmax(0,.92fr) minmax(0,1.08fr);gap:12px;margin-top:13px;position:relative;z-index:1}.ct-market-panel{border:1px solid rgba(255,255,255,.07);border-radius:11px;background:rgba(0,0,0,.15);padding:11px}.ct-market-panel h3{margin:0 0 8px;font-size:12px;letter-spacing:.05em}.ct-market-role,.ct-market-job{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:9px;padding:9px 0;border-top:1px solid rgba(255,255,255,.06)}.ct-market-role:first-of-type,.ct-market-job:first-of-type{border-top:0}.ct-market-role strong,.ct-market-job strong{display:block;font-size:12px}.ct-market-role small,.ct-market-job small{display:block;color:var(--muted,#9eb1bf);font-size:10px;line-height:1.4;margin-top:3px}.ct-market-status{font:800 9px/1 ui-monospace,monospace;border:1px solid rgba(71,232,208,.25);border-radius:999px;padding:5px 7px;white-space:nowrap;height:max-content}.ct-market-links{display:flex;gap:6px;flex-wrap:wrap;margin-top:5px}.ct-market-links a,.ct-market-refresh{font:700 9px/1.2 ui-monospace,monospace;border:1px solid rgba(255,255,255,.1);border-radius:7px;padding:5px 7px;color:inherit;text-decoration:none;background:rgba(0,0,0,.17);cursor:pointer}.ct-market-live{display:flex;justify-content:space-between;gap:9px;align-items:center;margin-bottom:6px}.ct-market-live small{color:var(--muted,#9eb1bf)}
    .ct-market-focus{position:relative;z-index:1;margin-top:12px;border:1px solid rgba(71,232,208,.16);border-radius:11px;background:rgba(2,12,17,.25);overflow:hidden}.ct-market-focus>summary{cursor:pointer;list-style:none;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center;padding:11px 12px}.ct-market-focus>summary::-webkit-details-marker{display:none}.ct-market-focus-title strong{display:block;font-size:12px}.ct-market-focus-title span{display:block;color:var(--muted,#9eb1bf);font-size:10px;line-height:1.4;margin-top:3px}.ct-market-focus-body{border-top:1px solid rgba(255,255,255,.07);padding:12px}.ct-market-focus-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.ct-market-focus-card{border:1px solid rgba(255,255,255,.06);border-radius:9px;padding:10px;background:rgba(0,0,0,.12)}.ct-market-focus-card h4{margin:0 0 7px;font-size:10px;letter-spacing:.08em;color:var(--accent,#47e8d0)}.ct-market-focus-card ul{margin:0;padding-left:17px;color:var(--muted,#9eb1bf);font-size:11px;line-height:1.45}.ct-market-focus-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:8px;padding:7px 0;border-top:1px solid rgba(255,255,255,.05)}.ct-market-focus-row:first-child{border-top:0}.ct-market-focus-row strong{font-size:11px}.ct-market-focus-row span,.ct-market-focus-row small{font-size:9px;color:var(--muted,#9eb1bf)}.ct-market-callout{margin-top:9px;border-left:2px solid var(--accent,#47e8d0);padding:8px 9px;background:rgba(71,232,208,.045);font-size:11px;line-height:1.45;color:var(--muted,#9eb1bf)}
    @media(max-width:760px){.ct-market-head{display:block}.ct-market-meta{justify-content:flex-start;margin-top:9px}.ct-market-grid,.ct-market-focus-grid{grid-template-columns:1fr}.ct-market-now{padding:12px}.ct-market-role,.ct-market-job,.ct-market-focus>summary{grid-template-columns:1fr}.ct-market-status{width:max-content}.ct-market-focus-body{padding:10px}}
  `;document.head.appendChild(s);}
  function passedStats(){const passed=CERTS.filter(c=>state.passes?.[c.id]).length,progress=CERTS.filter(c=>!state.passes?.[c.id]&&Number(CT.store.objective?.(c.id)||0)>0).length;return{passed,progress};}
  function liveObserved(value,feed){const candidates=[...(value.applyNow||[]),...(value.stretch||[])].slice(0,5),bands=candidates.map(role=>CT.jobMarket.liveBand(role,feed)).filter(Boolean);if(!bands.length)return null;return{low:Math.min(...bands.map(x=>x.low)),high:Math.max(...bands.map(x=>x.high)),samples:bands.reduce((s,x)=>s+x.samples,0)};}
  function roleHtml(role,feed){const live=CT.jobMarket.liveBand(role,feed),band=live||CT.marketReadiness.adjustedRange(role),range=`${money(band.low)}–${money(band.high)}`;return `<div class="ct-market-role"><div><strong>${esc(role.label)}</strong><small>${role.score}% overall · market ${role.marketAccess}% · capability ${role.capability}% · ${esc(range)}${live?` · ${live.samples} current salary samples`:''}</small><small>${esc(role.missing)}</small><div class="ct-market-links"><a href="${esc(CT.jobMarket.providerSearch(role,'indeed'))}" target="_blank" rel="noopener noreferrer">Indeed search</a><a href="${esc(CT.jobMarket.providerSearch(role,'reed'))}" target="_blank" rel="noopener noreferrer">Reed search</a><a href="${esc(CT.jobMarket.providerSearch(role,'adzuna'))}" target="_blank" rel="noopener noreferrer">Adzuna search</a></div></div><span class="ct-market-status">${esc(role.status)}</span></div>`;}
  function jobHtml(row){const j=row.job,salary=row.salaryMid?money(row.salaryMid):'salary not listed',source=j.source==='personal'?'<small>Your recorded vacancy</small>':'<a href="https://www.adzuna.co.uk/" target="_blank" rel="noopener noreferrer" aria-label="Jobs by Adzuna" style="display:inline-flex;align-items:center;gap:6px;min-width:116px;min-height:23px;margin-top:8px;font-size:12px;color:inherit">Jobs by <img src="https://upload.wikimedia.org/wikipedia/commons/5/51/Adzuna_Logo.png" alt="Adzuna" width="90" height="23" loading="lazy" referrerpolicy="no-referrer" style="object-fit:contain;background:white"></a>';return `<div class="ct-market-job"><div><strong>${esc(j.title)}</strong><small>${esc(j.company||'Employer not supplied')} · ${esc(j.location||'UK')} · ${salary}</small><small>${row.score}% fit · mapped to ${esc(row.role?.label||'role pathway')} · ${row.roleMatch}% title/skill match</small>${source}</div>${j.url?`<a class="ct-market-refresh" href="${esc(j.url)}" target="_blank" rel="noopener noreferrer">Open job</a>`:''}</div>`;}
  function certGapHtml(row){return `<div class="ct-market-focus-row"><div><strong>${esc(row.code||row.name)}${row.code?` · ${esc(row.name)}`:''}</strong><small>${row.progress}% progressed · ${esc(row.timing)} · ${esc(row.tandem)}${row.reason?` · ${esc(row.reason)}`:''}</small></div><span>M${row.M}/K${row.K}</span></div>`;}
  function evidenceGapHtml(row){return `<div class="ct-market-focus-row"><div><strong>${esc(row.evidence)}</strong><small>${esc(row.label)}</small></div><span>${esc(row.level)}</span></div>`;}
  function activeFocusHtml(focus,feed){if(!focus)return'';const live=CT.jobMarket.liveBand(focus.role,feed),range=live||focus.range,liveJobs=CT.jobMarket.rankJobs(feed,[focus.role]).filter(x=>x.roleMatch>=22),next=focus.nextCerts.slice(0,5),evidence=focus.evidenceGaps.slice(0,5);return `<details class="ct-market-focus" data-market-focus open><summary><div class="ct-market-focus-title"><strong>ACTIVE ROLE FILTER · ${esc(focus.label)}</strong><span>${focus.score}% role readiness · ${focus.status} · ${focus.progress.passed}/${focus.progress.total} certs passed · ${focus.progress.inProgress} in progress · ${liveJobs.length} matching current listings</span></div><div class="ct-market-meta"><span class="ct-market-chip">M ${focus.marketAccess}%</span><span class="ct-market-chip">K ${focus.capability}%</span><span class="ct-market-chip">FLOOR ${focus.pillarFloor}%</span><span class="ct-market-chip">${money(range.low)}–${money(range.high)}</span></div></summary><div class="ct-market-focus-body"><div class="ct-market-focus-grid"><section class="ct-market-focus-card"><h4>WHAT THE ROLE REQUIRES</h4><ul>${focus.responsibilities.slice(0,6).map(x=>`<li>${esc(x)}</li>`).join('')}</ul></section><section class="ct-market-focus-card"><h4>CURRENT PATHWAY STATE</h4><div class="ct-market-sub">${focus.progress.weighted}% certification-path progress · ${focus.progress.passed} passed · ${focus.progress.inProgress} actively progressed · ${focus.progress.unstarted} unstarted.</div><div class="ct-market-callout">${esc(focus.applicationGuidance)}</div></section><section class="ct-market-focus-card"><h4>NEXT CERTIFICATION GAPS</h4>${next.length?next.map(certGapHtml).join(''):'<div class="ct-market-sub">No unpassed certifications remain in this pathway. Shift attention to USED / DESIGNED / OWNED evidence.</div>'}</section><section class="ct-market-focus-card"><h4>PRACTICAL EVIDENCE GAPS</h4>${evidence.length?evidence.map(evidenceGapHtml).join(''):'<div class="ct-market-sub">No major evidence gaps remain below DESIGNED level for the mapped capability pillars.</div>'}</section></div><div class="ct-market-callout"><strong>Current constraint:</strong> ${esc(focus.limitingMessage)} The dashboard deliberately uses the weaker pillar as the brake: a strong CV signal does not make this role ready when practical capability is lagging, and deep capability does not produce market value if employers cannot recognise it.</div></div></details>`;}
  function render(feed){feed=CT.careerMentor?.combinedFeed(feed)||feed;const value=CT.marketReadiness.currentValue(),focus=CT.marketReadiness.activeAssessment(),stats=passedStats(),observed=liveObserved(value,feed),focusLive=focus?CT.jobMarket.liveBand(focus.role,feed):null,displayRange=focus?(focusLive||focus.range):{low:value.low,high:value.high},roles=focus?[focus.role,...value.roles.filter(r=>r.id!==focus.role.id).slice(0,4)]:[...(value.applyNow||[]),...(value.stretch||[]),...(value.next||[])].slice(0,6),market=CT.jobMarket.summary(feed),jobs=(focus?CT.jobMarket.rankJobs(feed,[focus.role]).filter(x=>x.roleMatch>=22):market.bestFit).slice(0,6),fresh=feed?.fetchedAt?new Date(feed.fetchedAt).toLocaleString('en-GB'):'not yet live',kicker=focus?`ACTIVE ROLE / ILLUSTRATIVE BAND · ${focus.label}`:'CAREER EVIDENCE / ILLUSTRATIVE BANDS',descriptor=focus?`The range below is scoped to the active role filter. It uses your passed/progressed certifications, mapped practical evidence, pathway requirements and current market observations when enough salary samples exist.`:'Uncalibrated illustrative UK application range from certifications marked passed, recorded certification progress and practical-evidence maturity. It is constrained by whichever is weaker: market access or ability to perform after hire.';return `<section class="ct-market-now" data-market-dashboard><div class="ct-market-head"><div><div class="ct-market-kicker">${esc(kicker)}</div><div class="ct-market-value">${focusLive||observed?money((focusLive||observed).low)+'–'+money((focusLive||observed).high):'Market pay not established'}</div><div class="ct-market-sub">${esc(descriptor)} <strong>Salary bands without current samples are illustrative model estimates, not observed market pay.</strong></div></div><div class="ct-market-meta"><span class="ct-market-chip">${stats.passed} PASSED</span><span class="ct-market-chip">${stats.progress} IN PROGRESS</span>${focus?`<span class="ct-market-chip">${focus.status}</span><span class="ct-market-chip">${focus.score}% EVIDENCE COVERAGE</span>`:`<span class="ct-market-chip">${value.applyNow.length} ROLES TO COMPARE</span>`}${focusLive?`<span class="ct-market-chip">${focusLive.samples} CURRENT SALARY SAMPLES</span>`:observed?`<span class="ct-market-chip">OBSERVED ${money(observed.low)}–${money(observed.high)}</span>`:''}</div></div>${activeFocusHtml(focus,feed)}<div class="ct-market-grid"><div class="ct-market-panel"><h3>${focus?'ACTIVE ROLE + ADJACENT REALISTIC TARGETS':'REALISTIC ROLE TARGETS NOW'}</h3>${roles.length?roles.map(r=>roleHtml(r,feed)).join(''):'<div class="ct-market-sub">No role has enough evidence yet. Build the current learning rung rather than applying blindly.</div>'}</div><div class="ct-market-panel"><div class="ct-market-live"><div><h3 style="margin:0">${focus?'RECENT ACTIVE-ROLE JOB SAMPLE':'RECENT MATCHING JOB SAMPLE'}</h3><small>${esc(feed?.provider||'No provider')} · refreshed ${esc(fresh)}</small></div><button type="button" class="ct-market-refresh" data-market-refresh>Refresh view</button></div>${jobs.length?jobs.map(jobHtml).join(''):`<div class="ct-market-sub">${feed?.status==='awaiting-provider-credentials'?'Automated provider data is unavailable. Add sourced vacancies in the Mentor Evidence Lab; role-readiness still works locally.':feed?.status!=='live'&&feed?.status!=='ready'?'Market evidence is unavailable or incomplete. This does not mean there are no jobs.':'No listings in this sample match the model threshold; wider demand is unknown.'}</div>`}<div class="ct-market-sub" style="margin-top:8px">Best-fit matching happens in your browser. Your passed certifications, progress, active filter and evidence are not sent to a jobs provider.</div></div></div></section>`;}
  function bind(host){host.querySelector('[data-market-refresh]')?.addEventListener('click',async event=>{const button=event.currentTarget,label=button.textContent;button.disabled=true;button.textContent='Refreshing…';try{await mount(true);}finally{if(button.isConnected){button.disabled=false;button.textContent=label;}}});}
  async function mount(force=false){
    style();if(state.currentTab!=='dashboard')return;
    const content=document.getElementById('tab-content');if(!content)return;
    if(!lastFeed){lastFeed={status:'loading',jobs:[],fetchedAt:null};CT.jobMarket.load().then(feed=>{lastFeed=feed;feedVersion++;queue();});}
    else if(force){lastFeed=await CT.jobMarket.load({force:true});feedVersion++;}
    // A feed may finish after navigation. Never insert into a detached or replaced workspace.
    if(!lastFeed||state.currentTab!=='dashboard'||document.getElementById('tab-content')!==content)return;
    const existing=content.querySelector('[data-market-dashboard]'),inputs=JSON.stringify([state,revision,feedVersion]);
    if(!force&&existing&&dashboardInputs.get(existing)===inputs)return;
    const html=render(lastFeed);
    // Preserve focus, expanded details and rank badges when observer notifications change no data.
    if(existing&&dashboardMarkup.get(existing)===html){dashboardInputs.set(existing,inputs);return;}
    const host=document.createElement('div');host.innerHTML=html;const next=host.firstElementChild;
    dashboardMarkup.set(next,html);
    dashboardInputs.set(next,inputs);
    if(existing)existing.replaceWith(next);else{const brief=content.querySelector('.ct-dual-brief');if(brief)brief.insertAdjacentElement('afterend',next);else content.prepend(next);}
    bind(next);
  }
  let queued=false;function queue(){if(queued)return;queued=true;queueMicrotask(()=>{queued=false;mount(false);});}
  const app=document.getElementById('app');if(app)new MutationObserver(queue).observe(app,{childList:true,subtree:true});global.addEventListener('certtracker:career-context-changed',queue);global.addEventListener('certtracker:capability-evidence-changed',queue);global.addEventListener('certtracker:goal-changed',queue);if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',queue,{once:true});else queue();
  CT.marketDashboardUI=Object.freeze({render,mount});
})(window);
