# Sync hardening complete

This branch finalises the privacy model for browser cross-device sync: dedicated private state repository, client-side encryption, session-only secrets, remote-first initial binding, conflict blocking and regression coverage.
