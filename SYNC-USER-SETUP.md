# User setup

To activate cross-device sync, create a dedicated private GitHub repository for encrypted state, create a fine-grained token limited to that repository with Contents read/write, then configure the Sync panel on the canonical device and use Smart sync. See DEVICE-SYNC.md for the full procedure.
